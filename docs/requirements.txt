# tkfluent 文档站的构建依赖。
#
# 之所以单独列出并**锁版本**：这套工具链的版本组合相当敏感。
# 例如 mkdocstrings-python 2.x 在较旧的 pymdown-extensions 上会崩在
# pygments 的 `filename=None`（AttributeError: 'NoneType' object has no attribute 'replace'），
# 而 mkdocs-material 9.x 又要求 pymdown-extensions 不低于某个版本。
#
# 安装：
#     pip install -r docs/requirements.txt
#
# 构建：
#     mkdocs build --strict -f docs/mkdocs.yml
# 本地预览：
#     mkdocs serve -f docs/mkdocs.yml

mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1

# API 文档由源码 docstring 自动生成
mkdocstrings[python]==1.0.6
mkdocstrings-python==2.0.8

# 代码块高亮；mkdocstrings 会用到 pymdownx.highlight
pymdown-extensions>=11.0,<12
Pygments>=2.20

# 博客 RSS（mkdocs.yml 里的 rss 插件）
mkdocs-rss-plugin==1.19.0

# mkdocs.yml 的 blog 功能由 mkdocs-material 自带，无需额外安装
